**Description**
Hello, this is a Note App made using React

**Installation**
First make sure npm installed
Then in the terminal run npm install in the my-app directory 
If offline use npm install --offline 
Then run npm run dev while still in the my-app directory 
Finally open the localhost link to use the app

